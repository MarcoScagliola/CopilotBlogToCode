# TODO

1. Replace placeholder Databricks workspace URL output with actual workspace provisioning or data source lookup.
2. Add Unity Catalog object provisioning (catalogs, schemas, grants) using Databricks provider.
3. Add per-environment table bootstrap and idempotent DDL.
4. Add private networking and Key Vault RBAC-only model.
5. Add terraform plan job and policy checks in CI.
6. Add smoke tests for deploy_dab.py with fixture outputs.
